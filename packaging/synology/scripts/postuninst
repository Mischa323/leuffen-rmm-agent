#!/bin/sh
# Tidy up runtime files + the sudo grant; DSM removes the package var area itself.
PKG="LeuffenRMM"
VAR="${SYNOPKG_PKGVAR:-/var/packages/${PKG}/var}"
rm -f "${VAR}/agent.pid" "${VAR}/agent.log"
rm -f /etc/sudoers.d/leuffen-rmm
exit 0
